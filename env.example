# Port the server listens on. Render sets this automatically — you
# generally don't need to set it yourself in production.
PORT=5000

# Lock CORS down to your deployed frontend origin once you have one,
# e.g. https://bun-fight.onrender.com. Defaults to "*" (any origin),
# which is fine for local dev.
CORS_ORIGIN=*
